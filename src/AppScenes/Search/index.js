import React from 'react';
import {
  StyleSheet,
  View,
  Text,
} from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
  },
});

export const Search = () => (
  <View style={styles.wrapper}>
    <Text>SEARCH</Text>
  </View>
);
