import React from 'react';
import {
  StyleSheet,
  View,
  Text,
} from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
  },
});

export const Contact = () => (
  <View style={styles.wrapper}>
    <Text>Contact</Text>
  </View>
);
